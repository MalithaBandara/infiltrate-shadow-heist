android.useAndroidX=true
# Default 512MB heap isn't enough to dex the full Compose + KorGE + play-services-ads dependency
# graph ("Gradle build daemon has been stopped: since the JVM garbage collector is thrashing").
org.gradle.jvmargs=-Xmx3072m
